import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: 'export',
  basePath: '/springAiReactAgent',
  assetPrefix: '/springAiReactAgent',
  trailingSlash: true,
};

export default nextConfig;
